apiVersion: v2
name: control-plane
description: Iterabase control-plane service (substrate + durable engine). Stub — not yet implemented (HOR-241+).
type: application
version: 0.1.0
appVersion: "0.1.0"
