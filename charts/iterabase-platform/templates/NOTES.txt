iterabase platform {{ .Chart.AppVersion }} installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Components:
  inference-gateway: {{ (index .Values "inference-gateway").enabled }}
  control-plane:     {{ (index .Values "control-plane").enabled }} (stub)
  agent-fleet:       {{ (index .Values "agent-fleet").enabled }} (stub)
  postgresql:        {{ .Values.postgresql.enabled }}
  redis:             {{ .Values.redis.enabled }}
  minio:             {{ .Values.minio.enabled }}
  ingress-nginx:     {{ (index .Values "ingress-nginx").enabled }}

Get the gateway admin API key:
  kubectl get secret {{ .Release.Name }}-gateway-admin -n {{ .Release.Namespace }} -o jsonpath='{.data.adminApiKey}' | base64 -d
